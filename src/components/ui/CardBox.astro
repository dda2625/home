---
const { title, padding } = Astro.props;
---

<div class="flex flex-col col-span-6 xl:col-span-4">
    <div class="bg-secondary h-12 text-lg font-bold text-white w-full m-0 pl-4 flex items-center">
        {title}
    </div>
    <div class={`bg-white p-${padding}`}>
        <slot></slot>
    </div>
</div>