---
import Layout from "../../layout/Layout.astro";
const { SiteTitle } = Astro.props;
---
<Layout SiteTitle={`${SiteTitle} | VATSIM Scandinavia`}>
    <div class="max-w-[1300px] max-xl:p-4 w-full min-h-screen flex flex-col justify-start">
        <slot/>
    </div>
</Layout>